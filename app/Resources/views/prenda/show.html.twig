{% extends 'base.html.twig' %}

{% block body %}
    <h1>Prenda</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prenda.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ prenda.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('prenda_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('prenda_edit', { 'id': prenda.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
